

const initialState = {
    isLoading: false,
    error: '',
    //TODO: insert shape of data from api
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {


        default: 
            return state;
    }
}