import { connect } from 'react-redux';

const SearchPodForm = (props) => {

    return (
        <div>
            Form to go here
        </div>
    )
}

export default connect(null, {})(SearchPodForm)